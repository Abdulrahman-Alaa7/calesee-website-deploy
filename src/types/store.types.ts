export type ColorType = {
  id: string;
  hex: string;
  nameEn: string;
  nameAr: string;
  createdAt: string;
  updatedAt: string;
};

export type SizeType = {
  id: string;
  valueSize: string;
  labelEn: string;
  labelAr: string;
  createdAt: string;
  updatedAt: string;
};
